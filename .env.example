# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/student_teacher_appointment

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Server Configuration (use 5000 so frontend can connect without changing its default)
PORT=5000
NODE_ENV=development
HOST=0.0.0.0

# User approval: leave unset or true so students/teachers can login. Set to false to require admin approval.
# AUTO_APPROVE_USERS=false

# Frontend URL (for password reset emails etc.)
FRONTEND_URL=http://localhost:3000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
